pub fn reverse_vowels(s: String) -> String {
    let vowels = ['a','e','i','o','u','A','E','I','O','U'];
    //I should go down the string, find the i values where there is a value, use a for loop, then store those values and reverse them,
    //maybe use match to make a dictionary from i values to vowels?
}

///this feels like such a slow solution!


//////testing


fn main() {
    let Strng= ".";
    println!("{:?}", reverse_vowels(Strng);
}